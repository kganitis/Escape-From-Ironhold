# Available commands:
# look {direction}? => default:around
# go {direction}|{location}
# examine {item}?|{location}? => default:current location
# take {item}
# use {item}+
# combine {item} {item}+
# fight {character}
# wait
# help
# exit